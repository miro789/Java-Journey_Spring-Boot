spring.application.name=miro-do-java-1
spring.profiles.active=@spring.profiles.active@
server.port=9090

open.api.title=Miro API
open.api.version=v1.0.0
open.api.description=Description API service
open.api.serverUrl=http://localhost:9090
open.api.serverName=Server test

# default is 10
spring.datasource.hiraki.maximum-pool-size=20
# default is same as max pool size
spring.datasource.hiraki.minimum-idle=10
# default is 30 seconds
spring.datasour.hikari.connection-timeout=3000
#default is 600000 i.e 10 minutes
spring.datasource.hiraki.idle-timeout=600000
#default is 1800000 i.e 30 minutes
spring.datasource.hiraki.max-lifetime=1800000
spring.datasource.hiraki.pool-name=HirakiConnPool

# EMAIL SENDER
spring.mail.from=${MAIL_FROM:doanlehuyen935@gmail.com}
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true